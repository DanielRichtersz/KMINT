#include "kmint/map/map.hpp"

namespace kmint {
	namespace pigisland {
		class BaseState {
		public:
			//BaseState(const play::map_bound_actor* actor) : _actor(actor) {}
			//BaseState() {}
			//virtual BaseEnduranceState replaceState() { return BaseEnduranceState{ 100 }; };
			//virtual const map::map_node* Execute(const play::map_bound_actor* actor);
		private:
			//const play::map_bound_actor* _actor;
		};
	}
}